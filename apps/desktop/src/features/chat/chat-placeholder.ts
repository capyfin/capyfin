/**
 * Static placeholder text for the chat input.
 * Replaces the previous pattern that exposed internal agent names.
 */
export const CHAT_INPUT_PLACEHOLDER = "Ask anything about markets...";

/**
 * Static subtitle for the chat empty state.
 * Replaces the previous pattern that exposed internal agent names (e.g. "Ask Main about...").
 */
export const CHAT_EMPTY_STATE_SUBTITLE =
  "Ask about markets, analysis, and finance decisions.";
